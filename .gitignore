# Terraform providers / modules (don't commit)
.terraform/

# Terraform state files (must NOT be committed)
terraform.tfstate
terraform.tfstate.*
*.tfstate
*.tfstate.*

# IDE files
.idea/
*.iml

# Crash logs
crash.log

# Local override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform variable files (sensitive)
*.tfvars
*.tfvars.json

